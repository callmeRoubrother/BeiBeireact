import { connect } from 'react-redux'
import UI from './UI'

const Com = connect(
    (state) => {
      return {

      }
    },
    (dispatch) => {
      return {

      }
    }
)(UI)

export default Com